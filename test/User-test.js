import chai from 'chai';
import User from '../src/User.js';
const expect = chai.expect;

describe('User', () => {
  before(() => {
    const user = new User();
  })
  it('should be a function', () => {
    expect()
  })
})
